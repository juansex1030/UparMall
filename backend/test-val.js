const { ValidationPipe } = require('@nestjs/common'); const { UpdateSettingDto } = require('./dist/settings/dto/update-setting.dto.js'); async function run() { const pipe = new ValidationPipe({ whitelist: true, transform: true }); const payload = { businessHours: [{ day: 'Lunes', open: '08:00', close: '18:00', enabled: true }], extra: 'will-be-stripped' }; try { const result = await pipe.transform(payload, { type: 'body', metatype: UpdateSettingDto }); console.log('Result:', JSON.stringify(result)); } catch (e) { console.error(e); } } run();